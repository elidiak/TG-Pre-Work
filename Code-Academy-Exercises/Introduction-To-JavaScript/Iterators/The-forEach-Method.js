const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
function iWantToEat(element) {
  console.log('I want to eat a ' + element);
}

//Now run the earlier bit over the array
fruits.forEach(iWantToEat);
